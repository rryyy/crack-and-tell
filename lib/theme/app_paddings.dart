import 'package:flutter/material.dart';

class AppPadding {
  static const EdgeInsets screen = EdgeInsets.all(16);
  static const EdgeInsets tabBarScreen = EdgeInsets.only(left: 16, right: 16, top: 16);
  static const EdgeInsets card = EdgeInsets.all(20);
  static const EdgeInsets horizontal = EdgeInsets.symmetric(horizontal: 16);
}